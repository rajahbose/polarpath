# Git Auto-Push Rule

After completing any code changes or fixes requested by the user, automatically stage all changes, commit them with a descriptive message, and push them to `origin/main` without waiting for a separate user prompt.
